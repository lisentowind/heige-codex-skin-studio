# v5.4.0 悟空双主题发布验证

## 候选版本

- 分支：`codex/add-goku-presets`
- 验证提交：`83bd25b5f6cc4ffead3fc0a7fd0af89e09961a8b`
- 版本：`5.4.0`
- 制品：`output/heige-codex-skin-studio.skill`
- SHA-256：`0537e9a4eea1494310df8074b50ca7ba703bfe4ca90a3fd47f1f2816e380ec5b`

## 输入与输出

| 主题 | 原始输入 SHA-256 | WebP 尺寸 | WebP 大小 |
| --- | --- | --- | --- |
| 龙珠 · 筋斗云 | `e6d2ef7141a0735474e6c4dac96c6875da0c7a2f59fa4f489093cca26fa774b4` | 1600×900 | 160720 bytes |
| 龙珠 · 超级赛亚人 | `c994002195210149abdfdfe872b578e385670036576d42995d5ce4f95a723880` | 1600×900 | 177066 bytes |

原始 PNG 在转换前后 SHA-256 一致，未被修改。两个主题只新增 `theme.json` 与 `hero.webp`，没有新增运行时代码、依赖、监听器、网络请求或后台任务。

## 自动化测试

最终发布候选执行 `npm test`：

- tests：988
- pass：982
- fail：0
- skipped：6
- duration：53729 ms

6 项跳过均为现有明确环境门禁，包括需要显式 opt-in 的破坏性 macOS 真机生命周期测试和需要 Windows 环境的真实 Node CLI 测试。本次没有把跳过项当成跨平台真机通过。

其他门禁：

- `node --test test/bundled-presets.test.mjs`：1/1 通过。
- `node --test test/release-governance.test.mjs test/product-identity.test.mjs`：12/12 通过。
- `npm run release:check`：39 个视觉素材全部登记并满足公开发布状态门禁。
- 两次固定时间戳构建 SHA-256 均为 `0537e9a4eea1494310df8074b50ca7ba703bfe4ca90a3fd47f1f2816e380ec5b`，字节完全一致。
- `unzip -t`：无压缩错误。
- 归档内 `.command` 与 `.zsh` 入口为 `0755`，两个主题清单和图片为 `0644`。

## macOS 安装与真机验收

从候选 `.skill` 解压后执行其安装入口，安装事务返回：

```json
{
  "decision": "commit",
  "persistenceEnabled": true,
  "recovered": false,
  "targetRoot": "/Users/blakexu/.codex/heige-codex-skin-studio"
}
```

安装后的 `package.json` 版本为 `5.4.0`，CLI 列表包含 12 个内置主题和两个新 ID：

- `dragonball-nimbus`
- `dragonball-super-saiyan`

依次真实应用两个主题后，主 renderer 状态均为 `installed: true`、`mode: active`、`menu: true`、`failed: []`。最终状态为：

```json
{
  "themeId": "dragonball-super-saiyan",
  "persistenceEnabled": true,
  "revision": 147
}
```

`doctor` 结果：

- Codex Desktop：`26.715.31925`
- 调试端口：`127.0.0.1:9341`
- `processHasDebugFlag: true`
- `portOpen: true`
- diagnosis：`ok：端口开放，可直接注入`

CDP 计算样式检查：

| 主题 | 阅读增强 | 回复底色 | 文字色 | 高斯模糊 |
| --- | --- | --- | --- | --- |
| 龙珠 · 筋斗云 | `on` | 冷白 90％ | `rgb(20, 33, 61)` | `none` |
| 龙珠 · 超级赛亚人 | `on` | 暖白 90％ | `rgb(40, 32, 51)` | `none` |

真机首轮曾发现深色文字方案让 Codex 原生白色信息卡出现低对比度。发布候选已改为浅色表面与深色文字，补充预设合同后重新打包、安装、切换和截图验证，问题不再出现。

## 未越界声明

Windows 自动化仍由仓库的 GitHub Actions PowerShell 5.1、PowerShell 7、32 位解析、确定性制品与 Scheduled Task 门禁负责。当前 Mac 无法把 Microsoft Store/MSIX 真机链路宣称为已验证，Release 继续保留该真实设备边界。

两张图由项目所有者提供并明确要求公开发布。`ASSET_PROVENANCE.md` 已如实记录第三方角色形象缺少独立可核验授权文件的边界，项目所有者确认公开发布不等同于第三方法律意见或商业授权证明。
